{
  "description": "a Static local keeps its value between calls and is a symbol of its procedure",
  "status": "known-gap",
  "gap": "procedure-local declarations (Dim/Static/Const) are not extracted as nodes (prompt §8 Variable)",
  "expect": {
    "nodes": [
      {
        "kind": "variable",
        "name": "callCount"
      }
    ],
    "edges": [
      {
        "kind": "contains",
        "from": "Tick",
        "to": "callCount"
      }
    ]
  }
}
